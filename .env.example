# Zinc.ai Environment Variables

# AI Provider (choose one: 'anthropic' or 'openai')
AI_PROVIDER=anthropic
# AI_MODEL=claude-sonnet-4-5-20250514  # Optional — overrides default model

# Anthropic API Key (for Claude)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# OpenAI API Key (for GPT)
OPENAI_API_KEY=your_openai_api_key_here

# Slack Bot (required — see README for setup)
# Scopes needed: chat:write, chat:write.customize, im:history, im:read,
# im:write, users:read, users:read.email, channels:read, channels:history, app_mentions:read
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_APP_TOKEN=xapp-your-app-token

# Zinc Cat bot avatar (public URL to a PNG — shown on every message)
# Upload your logo somewhere and paste the URL here
ZINC_CAT_ICON_URL=

# Moltbook Hub
HUB_URL=http://localhost:3100
HUB_PORT=3100
HUB_HOST=0.0.0.0
HUB_DB_PATH=./moltbook.db
COMPANY_ID=default

# CORS — comma-separated allowed origins (default: localhost only)
# CORS_ORIGINS=http://localhost:*,https://your-app.com

# Recall.ai (for meeting bot)
RECALL_AI_API_KEY=your_recall_api_key_here

# Development Mode
NODE_ENV=development
